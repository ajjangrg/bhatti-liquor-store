package com.ajjan.liquorstore.Bll;

import com.ajjan.liquorstore.Api.updateApi;
import com.ajjan.liquorstore.Url.url;

public class updateBll {
    updateApi Update  = url.getInstance().create(updateApi.class);
}
