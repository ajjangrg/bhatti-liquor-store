<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ajjan.liquorstore">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <uses-permission android:name="android.hardware.sensor.proximity"/>
    <uses-permission android:name="android.permission.VIBRATE"/>


    <application
        android:allowBackup="true"
        android:icon="@drawable/app"
        android:label="@string/app_name"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@drawable/app"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name="com.ajjan.liquorstore.Cartlist_Activity"></activity>
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name="com.ajjan.liquorstore.MapActivity"
            android:label="@string/title_activity_map" />
        <activity android:name="com.ajjan.liquorstore.itemDetail_activity" />
        <activity android:name="com.ajjan.liquorstore.LogOut_activity" />
        <activity android:name="com.ajjan.liquorstore.HardDrinks_activity" />
        <activity android:name="com.ajjan.liquorstore.Beers_activity" />
        <activity android:name="com.ajjan.liquorstore.Wine_Activity" />
        <activity android:name="com.ajjan.liquorstore.UserProfileActivity" />
        <activity android:name="com.ajjan.liquorstore.Banner_Activity" />
        <activity android:name="com.ajjan.liquorstore.Item_List" />
        <activity android:name="com.ajjan.liquorstore.Dashboard_activity">
 </activity>
        <activity android:name="com.ajjan.liquorstore.Register_activity" />
        <activity android:name="com.ajjan.liquorstore.Splash_activity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>


        </activity>
        <activity android:name="com.ajjan.liquorstore.MainActivity" />
    </application>

</manifest>
